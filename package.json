{
  "name": "phonebook",
  "version": "0.0.1",
  "description": "Phonebook Exercises U of Helsinki Full Stack Open",
  "license": "ISC",
  "author": "Alex Skow",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../frontend_phone && npm run build && cp -r dist ../phonebook",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push -u origin main",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.6.2",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.4.1",
    "globals": "^17.6.0"
  }
}
